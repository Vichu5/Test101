# Test101
this is a dummy repo created to understand the basics of github and how git works, after finishing this i will be using the command /n
`git add <filename>`
and it will be updated after which i will run the command /n
`git push origin master`
